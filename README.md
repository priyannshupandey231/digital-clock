Creates the text box widget that renders the numbers on your screen. You can easily change the background and foreground parameters to customize your colors
: Fetches and parses your computer's system time. %I sets a 12-hour format, while %p handles the AM/PM notation. Change %I to %H if you prefer a 24-hour military clock instead
